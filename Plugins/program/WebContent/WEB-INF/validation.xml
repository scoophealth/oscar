<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE form-validation PUBLIC
          "-//Apache Software Foundation//DTD Commons Validator Rules Configuration 1.1.3//EN"
          "http://jakarta.apache.org/commons/dtds/validator_1_1_3.dtd">

<form-validation>

<!--
     This is a minimal Validator form file with a couple of examples.
-->

    <global>

        <!-- An example global constant
        <constant>
            <constant-name>postalCode</constant-name>
            <constant-value>^\d{6}\d*$</constant-value>
        </constant>
        end example-->

    </global>

    <formset>

        <!-- An example form -->
        <form name="logonForm">
            <field
                property="username"
                depends="required">
                    <arg key="logonForm.username"/>
            </field>
            <field
                property="password"
                depends="required,mask">
                    <arg key="logonForm.password"/>
                    <var>
                        <var-name>mask</var-name>
                        <var-value>^[0-9a-zA-Z]*$</var-value>
                    </var>
            </field>
        </form>
        
       <!-- program form -->
        <!-- form name="programManagerForm">
            <field
                property="program.name"
                depends="required">
                    <arg key="program.name"/>
            </field>
            <field
                property="program.descr"
                depends="required">
                    <arg key="program.descr"/>
            </field>
           <field
                property="program.location"
                depends="required">
                    <arg key="program.location"/>
            </field>
          <field
                property="program.type"
                depends="required">
                    <arg key="program.type"/>
            </field>
          <field
                property="program.maxAllowed"
                depends="required,integer">
                    <arg key="program.maxAllowed"/>
            </field>
        </form-->
<!--

        <form name="intakeAForm">
            <field
                property="clientFirstName"
	                depends="required">
                    <arg key="intakeAForm.clientFirstName"/>
            </field>
            <field
                property="clientSurname"
	                depends="required">
                    <arg key="intakeAForm.clientSurname"/>
            </field>
        </form>


        <form name="intakeBForm">
            <field
                property="clientFirstName"
	                depends="required">
                    <arg key="intakeBForm.clientFirstName"/>
            </field>
            <field
                property="clientSurname"
	                depends="required">
                    <arg key="intakeBForm.clientSurname"/>
            </field>
        </form>
-->
    </formset>

</form-validation>
